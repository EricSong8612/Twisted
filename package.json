{
  "name": "Twisted",
  "description": "twisted marketing site",
  "version": "1.0.0",
  "author": "Waverly & Co",
  "dependencies": {
    "contentful": "^4.6.0",
    "gatsby": "^1.8.10",
    "gatsby-link": "^1.6.5",
    "gatsby-plugin-react-helmet": "^1.0.1",
    "gatsby-source-contentful": "^1.3.1"
  },
  "keywords": [
    "gatsby"
  ],
  "license": "MIT",
  "main": "n/a",
  "scripts": {
    "build": "gatsby build",
    "develop": "gatsby develop",
    "format": "prettier --trailing-comma es5 --no-semi --single-quote --write 'src/**/*.js'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "prettier": "^1.5.2"
  }
}
